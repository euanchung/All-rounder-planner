{
  "name": "byeonsaeng-radar",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "공지 변경을 과제와 학습 계획에 연결하는 변생 레이더",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/campus-app.js && node --check src/campus-ui.js && node --check api/campus.js && node --check api/tutor.js && node --check src/engine.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@ai-sdk/google": "^4.0.64",
    "@neondatabase/auth": "^0.5.0-beta",
    "@neondatabase/serverless": "^1.1.0",
    "ai": "^7.0.93",
    "jose": "^6.2.12",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "esbuild-wasm": "^0.28.2"
  }
}
